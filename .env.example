# ⚠️ Key SULA TIDAK dipakai di package sula-ai (package ini cuma UI).
# Salin key ke tempat yang benar:
#
# 1. Backend SULA (AI ASISTENT SRS) — validasi request yang bawa X-Sula-Key
#    Di folder "AI ASISTENT SRS", file .env:
#    SULA_API_KEYS=sk-warehouse-xxx,sk-iot-demo-yyy
#
# 2. Web yang pakai sula-ai (mis. warehouse-srs) — supaya widget kirim key
#    Di folder project (warehouse-srs), file .env:
#    VITE_SULA_KEY=sk-warehouse-xxx
#
# (Hapus atau abaikan file .env di folder sula-ai; isi di sini tidak dipakai saat npm install.)
